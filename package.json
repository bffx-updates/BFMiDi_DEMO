{
  "name": "bfmidi-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "preview": "python -m http.server 4173 -d dist"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "preact": "^10.23.0"
  }
}
